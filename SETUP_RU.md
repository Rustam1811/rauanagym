# 🔥 Быстрая настройка Firebase для rauanagym

## Проблема
Ошибка "API key not valid" возникает потому что в `.env.local` используются демо-credentials.

## Решение (5 минут)

### 1️⃣ Откройте настройки Firebase

Перейдите по ссылке:
```
https://console.firebase.google.com/project/rauanagym/settings/general
```

### 2️⃣ Найдите Web SDK конфигурацию

1. Прокрутите вниз до раздела **"Your apps"**
2. Если есть веб-приложение (иконка `</>`), кликните на него
3. Если нет, нажмите **"Add app"** → **Web** → название: "FitCoach Web"
4. Скопируйте значения из `firebaseConfig`

### 3️⃣ Обновите `.env.local`

Откройте файл `d:\IT\gym\.env.local` и замените строки на ваши реальные значения:

```env
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSy...ваш_настоящий_ключ
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=rauanagym.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=rauanagym
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=rauanagym.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=ваш_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=1:ваш_app_id
```

### 4️⃣ Перезапустите сервер

В терминале VS Code:
```bash
# Остановите сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
```

### 5️⃣ Включите Authentication

1. Откройте: https://console.firebase.google.com/project/rauanagym/authentication/providers
2. Кликните на **"Email/Password"**
3. Включите переключатель **"Enable"**
4. Нажмите **"Save"**

### 6️⃣ Создайте Firestore Database

1. Откройте: https://console.firebase.google.com/project/rauanagym/firestore
2. Нажмите **"Create database"**
3. Выберите **"Start in test mode"**
4. Выберите регион (например, `europe-west`)
5. Нажмите **"Enable"**

### 7️⃣ Включите Storage

1. Откройте: https://console.firebase.google.com/project/rauanagym/storage
2. Нажмите **"Get started"**
3. Выберите **"Start in test mode"**
4. Нажмите **"Done"**

## ✅ Готово!

После этого:
- Ошибка исчезнет
- Вы сможете создать аккаунт на http://localhost:3000
- Приложение будет работать полностью

## 📱 Полезные ссылки

- Консоль Firebase: https://console.firebase.google.com/project/rauanagym
- Локальное приложение: http://localhost:3000
- Детальная инструкция: откройте `public/firebase-setup.html` в браузере

## ❓ Если что-то не работает

1. Проверьте что все значения в `.env.local` без пробелов и кавычек
2. Убедитесь что перезапустили сервер после изменения `.env.local`
3. Проверьте что Email/Password authentication включен в Firebase Console
4. Откройте консоль браузера (F12) и посмотрите ошибки
